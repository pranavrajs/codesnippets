#include<stdio.h>

int main()
{
	long int t_case;
	long long int i,num,arr1[1001],arr2[1000];
	scanf("%lu",&t_case);
	while(t_case-->0)
	{
		scanf("%llu",&num);
		for (i = 0; i < num; ++i)
			scanf("%d",&arr1[i]);
		for (i = 0; i < num; ++i)
			scanf("%d",&arr1[i]);
		
	}
}